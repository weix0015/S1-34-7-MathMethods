public class MathMethods {
    public static void main(String[] args) {
        System.out.println(
                Math.random() +
                Math.round(4.3) +
                Math.floor(4.3) +
                Math.ceil(4.3) +
                Math.PI +
                Math.multiplyExact(5, 8) +
                Math.pow(5, 2) +
                Math.min(4, 8) +
                Math.max(4, 8) +
                Math.sqrt(16) +
                Math.addExact(3, 4));

        // random() er relavent fordi for at få et tilfældigt udfald.

        // abs, fordi den den giver den absolute udfald/værdi af x

        // pow(x, y) kan bruges ved grafer

        // round(x) kan bruges hvis man gerne vil have at den runder til den nærmeste heltal.

        // cos(x) kan bruges indenfor pythagoras
    }

}